<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yootk.oa.dao.GradeMapper">
  <resultMap id="BaseResultMap" type="Grade">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
  </resultMap>
  <sql id="Base_Column_List">
    id, title
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from grade
    where id = #{id}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from grade
    where id = #{id}
  </delete>
  <insert id="insert" parameterType="Grade">
    insert into grade (id, title)
    values (#{id}, #{title})
  </insert>
  <insert id="insertSelective" parameterType="Grade">
    insert into grade
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        id,
      </if>
      <if test="title != null">
        title,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id},
      </if>
      <if test="title != null">
        #{title},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="Grade">
    update grade
    <set>
      <if test="title != null">
        title = #{title},
      </if>
    </set>
    where id = #{id}
  </update>
  <update id="updateByPrimaryKey" parameterType="Grade">
    update grade
    set title = #{title}
    where id = #{id}
  </update>
</mapper>